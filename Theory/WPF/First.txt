----------------------------------WPF----------------------------------

верстка находится в файликах типа .XAML

DirectX - используется для прорисовки(поздно перешли на него), использует 2 типа прорисовки(типы совместимы):
1. DirectDraw - 2d графика;
2. Direct3D - 3х мерная графика;

Интернационализация(добавлена изначально) - I18N.(не только для текста, но и например для переносов).

DataBinding - отвечает за привязку данных.(передаем в таблицу объект, и он нормально отоюражается).


Silverlight - web-интеграция 